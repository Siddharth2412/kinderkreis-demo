__pycache__
*.pyc
.venv
# Never bake a local dev database into the image — it's meant to live only
# in the bind-mounted app/data volume (see docker-compose.yml), and would
# otherwise ship real user data if this image were ever pushed anywhere.
app/data/*.db
